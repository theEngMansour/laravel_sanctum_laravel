{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "serve": "concurrently \"php artisan serve\" \"npm run dev\"",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "concurrently": "^8.2.2",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
